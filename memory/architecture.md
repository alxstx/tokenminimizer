# Architecture
<!-- Loaded ON DEMAND. The deeper map an agent reads when it needs to navigate beyond AGENTS.md. Keep it factual and current. -->

## Module map
<!-- dir/module → responsibility, deeper than AGENTS.md. Note key files per module. -->
{{module → responsibility, key files}}

## Data / control flow
<!-- How a request / job / command flows through the system, end to end. -->
{{flow}}

## Key abstractions
<!-- The 3–7 concepts you must understand to work here (the core types, the main interfaces, the central loop). -->
{{abstraction → what it is, where it lives}}

## Glossary (domain terms)
{{term → meaning}}

## External dependencies & integrations
<!-- Services, APIs, queues, DBs, third-party SDKs the code relies on. -->
{{dependency → how/where it's used}}
